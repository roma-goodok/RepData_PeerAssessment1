read.dcf()
?read.dcf
swirl()
library(swirl)
swirl()
library(swirl)
swirl()
summary(cars)
plot(cars)
getwd()
setwd("C:/Data/Coursera/repdata-010/RepData_PeerAssessment1")
setwd("C:/Data/Coursera/repdata-010/RepData_PeerAssessment1")
R.Version()$version.string
Sys.info()["sysname"]
Sys.info()["release"]
rversion <- R.Version()$version.string
sysname <- Sys.info()["sysname"]
sys.release <- Sys.info()["release"]
dir()
if (!file.exists("data")) {
dir.create("data")
}
?unz
download.file(fileUrl, destfile = "./data/activity.zip", method = "curl")
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata/data/activity.zip"
download.file(fileUrl, destfile = "./data/activity.zip", method = "curl")
con <- unz("./data/activity.zip")
con <- unz("./data/activity.zip", "activity.csv")
isOpen(con)
close(con)
f <- open(con)
con <- unz("activity.csv", "./data/activity.zip")
f <- open(con)
con <- unz("./data/activity.zip", "./data/activity.zip")
f <- open(con)
con <- unz("./data/activity.zip", "activity.csv")
f <- open(con)
f <- read(con)
f <- readLines(con)
?write.csv
?writeLines
writeLines(f, file("./data/activity.csv"))
?file.exists
file.info("./data/activity.zip")
str(file.info("./data/activity.zip"))
file.info("./data/activity.zip")$ctime
zip.file.loaded <- ("./data/activity.zip")$ctime
zip.file.loaded <- file.info("./data/activity.zip")$ctime
zip.file.ctime <- file.info("./data/activity.zip")$ctime
zip.file.ctime <- as.characther(file.info("./data/activity.zip")$ctime)
zip.file.ctime <- as.character(file.info("./data/activity.zip")$ctime)
zip.file.ctime
dataSource.info <- paste0("unzipped from zip ", zip_filename, " ", zip.file.ctime)
zip_filename <- "./data/activity.zip"
dataSource.info <- paste0("unzipped from zip ", zip_filename, " ", zip.file.ctime)
dataSource.info
dataSource.info <- paste0("unzipped from zip ", zip_filename, ", created on", zip.file.ctime)
dataSource.info
dataSource.info <- paste0("unzipped from zip '", zip_filename, "', created on", zip.file.ctime)
dataSource.info
dataSource.info <- paste0(" data set '"csv_filename"', unzipped from zip '", zip_filename, "', created on", zip.file.ctime)
paste0(" data source '",csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on", zip.file.ctime, "")
csv_filename <- "./data/activity.csv"
dataSource.info <- paste0(" data source '",csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on", zip.file.ctime, "")
dataSource.info
Data source: `r dataSource.info`
#check have we loaded data:
if (!file.exists("data")) {
dir.create("data")
}
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata/data/activity.zip"
zip_filename <- "./data/activity.zip"
csv_filename <- "./data/activity.csv"
if !file.exists(csv_filename)
{
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
} else {
dataSource.info <- csv_filename
}
if (!file.exists("data")) {
dir.create("data")
}
fileUrl <- "https://d396qusza40orc.cloudfront.net/repdata/data/activity.zip"
zip_filename <- "./data/activity.zip"
csv_filename <- "./data/activity.csv"
if (!file.exists(csv_filename)
{
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
} else {
dataSource.info <- csv_filename
}
if (!file.exists(csv_filename)
{
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
} else
{
dataSource.info <- csv_filename
}
if (!file.exists(csv_filename)
{
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
}
else {
dataSource.info <- csv_filename
}
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
if (!file.exists(csv_filename)
{
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
}
else   dataSource.info <- csv_filename
if (!file.exists(csv_filename)
{
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
}
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
if (!file.exists(csv_filename) {
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
}
if (!file.exists(csv_filename)) {
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
} else   {
dataSource.info <- csv_filename
}
close(con)
close(con)
if (!file.exists(csv_filename)) {
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
close(con)
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
}
else   {
dataSource.info <- csv_filename
}
if (!file.exists(csv_filename)) {
# if we have not CSV file and have not ZIP file - try to get  them from web
if (!file.exists(zip_filename))
{
download.file(fileUrl, destfile = zip_filename, method = "curl")
}
zip.file.ctime <- as.character(file.info(zip_filename)$ctime)
# unzip and save to CSV file
con <- unz(zip_filename, "activity.csv")
f <- readLines(con)
writeLines(f, file(csv_filename))
close(con)
dataSource.info <- paste0(csv_filename,"', unzipped from zip '", zip_filename, "', downloaded on ", zip.file.ctime, "")
} else
{
dataSource.info <- csv_filename
}
con
